import { Meteor } from 'meteor/meteor';

//import api
import '/imports/api/server/information';
import '/imports/api/server/scanning';
import '/imports/api/server/setting';

Meteor.startup(() => {
  // code to run on server at startup
});
